# LLM Configuration
# Options: openai, anthropic, gemini, mistral, groq
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=...

# Security Configuration
# Set to 'true' to allow the verifier to execute code (DANGEROUS in production)
ENABLE_CODE_EXECUTION=false

# Optional: Supabase Service Role (if backend needs admin access)
SUPABASE_SERVICE_ROLE_KEY=...
